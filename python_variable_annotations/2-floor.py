#!/usr/bin/env python3
"""Module that computes floor of a float."""


import math


def floor(n: float) -> int:
    """Return the floor of a float.

    Args:
        n (float): number to floor

    Returns:
        int: floor value
    """
    return math.floor(n)
